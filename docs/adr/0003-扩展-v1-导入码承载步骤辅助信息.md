# 扩展 V1 导入码承载步骤辅助信息

状态：accepted

自定义配方的制作步骤需要新增步骤预计用时和步骤提示，并在配方导入导出中保留这些可制作信息。我们选择直接扩展 `COCKTAIL_V1:` 的步骤对象，而不是新增版本前缀，因为当前导入码仍处在早期本地文本传递阶段；V1 步骤对象承载 `instruction`、可选 `estimatedTime` 和可选 `tips`，步骤编号和动画仍由导入设备按本机规则补齐或省略。
